# Optional. Only set real values in Vercel — never commit live IDs.
NEXT_PUBLIC_GOOGLE_ADS_ID=
NEXT_PUBLIC_GOOGLE_ADS_CONVERSION_LABEL=

# Optional form delivery. The site builds and serves without these.
FORM_NOTIFY_EMAIL=
APPLY_WEBHOOK_URL=
ALIGN_WEBHOOK_URL=
GHL_LOCATION_ID=
GHL_APPLY_FORM_ID=
